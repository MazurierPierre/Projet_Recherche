class FrequencyBand:
    frequency = 100
    bandwidth = 50
    order = 3

    def __init__(self, frequency, bandwidth, order):
        self.frequency = frequency
        self.bandwidth = bandwidth
        self.order = order
